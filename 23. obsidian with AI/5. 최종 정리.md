



### 최종 진행 순서 v1.0 - OCI 이관용 Git 리셋

#### Step 1 - 기존 Git 분리

Bash

bash

```
cd ~/00_Newbeemv .git .git.old-$(date +%Y%m%d)git initgit branch -M main
# 확인ls -ld .git .git.old-*git status# .git.old-20260912 에 18GB 남아있어야 정상
```

4줄 숨겨짐

#### Step 2 - .gitignore 작성 (네 수정본 최종)

Bash

bash

```
cd ~/00_Newbee
cat > .gitignore <<'EOF'# macOS.DS_Store
# Obsidian - UI / cache.obsidian/workspace.json.obsidian/workspace-mobile.json.obsidian/cache/.obsidian/hotkeys.json.obsidian/appearance.json.obsidian/graph.json.obsidian/indexed-db/
# Lumina AI - 540MB.obsidian/plugins/lumina/storage/.obsidian/plugins/lumina/data.json
# Obsidian Git.obsidian/plugins/obsidian-git/data.json
# Syncthing.stfolder.stfolder.removed-*.stversions/.stignore
# Trash.trash/.obsidian-trash/
# Environment - 22M.smart-env/
# Large binaries - Gitea 터짐 방지*.bin*.tibx*.tib*.img*.iso*.zip*.7z*.tar*.apk*.ttc*.itb*.dmg*.exe**/assets/*.bin**/assets/*.tibx
# Temporary / log*.tmp*.logEOF
cat .gitignore
```

53줄 숨겨짐

#### Step 3 - 첫 커밋 전 용량 게이트 (필수)

Bash

bash

```
cd ~/00_Newbeegit add -A
echo "=== Git에 올라갈 총 용량 ==="git ls-files -z | xargs -0 du -ch 2>/dev/null | tail -1
echo "=== 파일 수 ==="git ls-files | wc -l
echo "=== 20MB 이상 파일 FAIL 체크 ==="git ls-files -z | xargs -0 -I{} sh -c 'test -f "$1" && du -h "$1"' _ "{}" 2>/dev/null | sort -hr | head -20
# 200~500MB면 PASS, 1GB 넘으면 여기서 멈추고 .gitignore 보완# PASS면 커밋git commit -m "init: 00_Newbee vault (history reset for OCI migration)"
```

10줄 숨겨짐

> 여기서 1GB 넘으면 push 하지 말고 멈춰. 결과 붙여줘.

#### Step 4 - Gitea 기존 저장소 삭제 (웹 UI)

`https://ocigit.duckdns.org` 접속

`david/00_newbee` > Settings > Danger Zone > Delete This Repository

이름 입력 후 삭제

#### Step 5 - Gitea 서버 디스크 정리 (OCI)

Bash

bash

```
# OCI 서버에서docker exec gitea ls /data/git/repositories/david/docker exec gitea rm -rf /data/git/repositories/david/00_newbee.gitdocker exec gitea rm -rf /data/repositories/david/00_newbee.git 2>/dev/nulldocker restart giteadf -h
```

1줄 숨겨짐

#### Step 6 - Gitea 빈 저장소 재생성

Gitea 웹 UI > `+` > New Repository

- Owner: `david`
- Name: `00_newbee`
- Initialize 전부 OFF (README, .gitignore, License 체크 해제)
- Create

#### Step 7 - Mac에서 push

Bash

bash

```
cd ~/00_Newbeegit remote add origin https://ocigit.duckdns.org/david/00_newbee.gitgit remote -vgit push -u origin main
```

#### Step 8 - OCI clone 테스트

Bash

bash

```
# OCI 서버에서cd ~rm -rf vault-testgit clone https://ocigit.duckdns.org/david/00_newbee.git vault-testdu -sh vault-testdu -sh vault-test/.git# 예상: 전체 200~500MB, .git 100~200MB
```

2줄 숨겨짐

#### Step 9 - obsidian-git 자동화 켜기

Obsidian 앱 > 설정 > Obsidian Git

- Auto Backup after file change: ON
- Auto Push Interval: 10
- Auto Pull Interval: 10

Bash

bash

```
cat ~/00_Newbee/.obsidian/plugins/obsidian-git/data.json | grep -E 'autoPush|autoPull'
```

#### Step 10 - 최종 OCI 구조 연결

Step 8에서 용량 정상 확인되면 내가 `docker-compose.yml` 최종본 바로 줄게:

Code

```
[Mac Git] -> Gitea:3000 -> /home/ubuntu/vault -> obsidian-mcp -> mcpo:8000 -> Open WebUI:8090 -> AI
```

---

지금 Step 1∼3까지 진행하고 `Step 3 용량 게이트` 결과만 보여줘. PASS면 바로 Step 4 가면 된다.