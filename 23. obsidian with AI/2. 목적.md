

```

현재 맥북을 사용중이고 옵시디언을 사용 중 입니다.  현재 목표는 ai 가 나의 옵시디언을 확인하고 정보를 얻고 정리 할 수 있게 하고 싶습니다.

네게 oci 무료 서버가 200기가 저장 용량으로 존재합니다. 이를 이용한다면 맥북이 아니라서 서버와 옵시디언을 동기화해서 서버에서 옵시디언을 조작하게 하는 건 어떤가요?
```



```

[맥북] ←Git→ [Gitea:3000] ←pull→ [/home/ubuntu/vault]
                                    ↓ (볼륨 마운트)
                          [obsidian-mcp] → [mcpo:8000] → [Open WebUI Tool]
====
좋습니다. 현재 내 맥북의 obsidian 설정을 터미널에서 확인 하고 정리부터 시작하는 건 어떨까요?


```

1. 현재 옵시디언 분석

```
cd ~/00_Newbee
pwd
ls -la
cat .obsidian/community-plugins.json 2>/dev/null || echo "community-plugins.json 없음"
du -sh .


cd ~/00_Newbee
git status 2>&1 | head -n 20
ls -la .obsidian/ | head -n 30


```


```
echo "=== 1. Obsidian Vault ==="
ls -ld ~/00_Newbee
du -sh ~/00_Newbee

echo
echo "=== 2. Obsidian 설정 ==="
ls -la ~/00_Newbee/.obsidian 2>/dev/null | head -30

echo
echo "=== 3. Obsidian CLI ==="
which obsidian 2>/dev/null
obsidian version 2>/dev/null
ls -l ~/.obsidian-cli.sock 2>/dev/null

echo
echo "=== 4. MCP 관련 프로세스 ==="
ps aux | grep -Ei 'mcp|obsidian' | grep -v grep
```


```
echo "=== 1. Obsidian Vault ==="
ls -ld ~/00_Newbee
du -sh ~/00_Newbee
echo "Top folders:"
du -sh ~/00_Newbee/* 2>/dev/null | sort -hr | head -n 20

echo
echo "=== 2. Obsidian 설정 ==="
ls -la ~/00_Newbee/.obsidian 2>/dev/null | head -n 30
cat ~/00_Newbee/.obsidian/community-plugins.json
echo
cat ~/00_Newbee/.gitignore
echo
echo "=== 2-1. Git 상태 ==="
cd ~/00_Newbee && git remote -v && git status --short | head -n 30

echo
echo "=== 3. Obsidian CLI & 플러그인 ==="
which obsidian 2>/dev/null
obsidian --version 2>/dev/null || obsidian version 2>/dev/null
ls -l ~/.obsidian-cli.sock 2>/dev/null
ls -1 ~/00_Newbee/.obsidian/plugins/ | grep -E "git|mcp|vault-as-mcp|lumina|fileorganizer"
cat ~/00_Newbee/.obsidian/plugins/obsidian-git/data.json 2>/dev/null | head -n 50
cat ~/00_Newbee/.obsidian/plugins/vault-as-mcp/data.json 2>/dev/null | head -n 80

echo
echo "=== 4. MCP 관련 프로세스 ==="
ps aux | grep -Ei 'mcp|obsidian' | grep -v grep

echo
echo "=== 5. 용량 폭탄 체크 (19GB 원인) ==="
du -sh ~/00_Newbee/.stversions ~/00_Newbee/.trash ~/00_Newbee/Clippings ~/00_Newbee/assets ~/00_Newbee/attachments 2>/dev/null
find ~/00_Newbee -type f -size +20M 2>/dev/null | head -n 20

echo
echo "=== 6. OCI 연결 테스트 ==="
# OCI IP는 네 Gitea IP로 바꿔서 테스트
# ping -c 1 YOUR_OCI_IP
```


```
cd ~/00_Newbee

echo "=== 1. Vault 전체 ==="
du -sh .

echo "=== 2. 최상위 폴더별 용량 ==="
du -sh .[^.]* * 2>/dev/null | sort -h

echo "=== 3. 실제 Markdown 용량 ==="
find . -name "*.md" -type f -exec du -ch {} + 2>/dev/null | tail -1

echo "=== 4. Markdown 파일 수 ==="
find . -name "*.md" -type f | wc -l

echo "=== 5. 첨부파일 / Clippings ==="
du -sh attachments Clippings 2>/dev/null

echo "=== 6. 주요 첨부파일 용량 ==="
find . -type f \( -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" -o -name "*.gif" -o -name "*.webp" -o -name "*.pdf" -o -name "*.mp4" -o -name "*.mov" -o -name "*.zip" -o -name "*.bin" -o -name "*.tibx" \) -exec du -ch {} + 2>/dev/null | tail -1

echo "=== 7. .git 전체 ==="
du -sh .git
du -sh .git/objects
du -sh .git/objects/pack 2>/dev/null

echo "=== 8. Git 실제 저장 파일 수 ==="
git count-objects -vH

echo "=== 9. Git 현재 상태 ==="
git status --short

echo "=== 10. Git 원격 저장소 ==="
git remote -v

echo "=== 11. .obsidian 전체 ==="
du -sh .obsidian

echo "=== 12. Lumina 전체 ==="
du -sh .obsidian/plugins/lumina
du -sh .obsidian/plugins/lumina/storage 2>/dev/null

echo "=== 13. Lumina storage 상세 ==="
ls -lh .obsidian/plugins/lumina/storage 2>/dev/null

echo "=== 14. .obsidian/plugins별 용량 ==="
du -sh .obsidian/plugins/* 2>/dev/null | sort -h

echo "=== 15. Vault에서 큰 파일 TOP 30 ==="
find . -type f -exec du -h {} + 2>/dev/null | sort -hr | head -30

echo "=== 16. Git 추적 중인 Lumina 파일 ==="
git ls-files '.obsidian/plugins/lumina/*' 2>/dev/null

echo "=== 17. Git 추적 중인 대형 파일 TOP 30 ==="
git ls-files -z | xargs -0 -r -I{} sh -c 'test -f "$1" && du -h "$1"' _ "{}" 2>/dev/null | sort -hr | head -30

echo "=== 18. Gitignore ==="
cat .gitignore

echo "=== 19. MCP :8765 LISTEN ==="
lsof -nP -iTCP:8765 -sTCP:LISTEN 2>/dev/null

echo "=== 20. MCP endpoint ==="
curl -i --max-time 5 http://127.0.0.1:8765/mcp 2>/dev/null

echo "=== 21. Obsidian 관련 프로세스 ==="
ps aux | grep -i '[O]bsidian' | head -20
```